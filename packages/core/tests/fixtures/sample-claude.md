---
name: sample-claude
description: Representative CLAUDE.md fixture for walking skeleton tests
---

# Project Rules

## Getting Started

Please make sure to read the docs at https://example.com/docs before editing `./src/index.ts`.

Use inline `code` carefully.

```ts
const value = 1;
export { value };
```

I would be happy to help you with your tasks. Basically, note that this is filler prose.

Error: something went wrong

git commit -m "feat(core): add compression engine"

## Security

WARNING: never expose API keys

## Workflow

Type `confirm` before running destructive operations with `--force`.

1. First step in the workflow
2. Second step in the workflow
3. Third step in the workflow
