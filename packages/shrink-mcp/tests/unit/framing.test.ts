import { describe, it } from "vitest";

describe("NdjsonReadBuffer", () => {
  it.todo("splits partial chunks on newline boundaries");
  it.todo("skips empty lines");
  it.todo("flush returns remainder without trailing newline");
});
