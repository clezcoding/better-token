import { describe, it } from "vitest";

describe("shrinkListResponse", () => {
  it.todo("MCP-01: compresses long descriptions on list responses");
  it.todo("D-07: keeps original description when validator fails");
});
