{
  "name": "better-token",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@better-token/core && npm run build --workspace=@better-token/shrink-mcp",
    "test": "npm run test --workspace=@better-token/core && npm run test --workspace=@better-token/shrink-mcp",
    "dev": "npm run dev --workspace=@better-token/core"
  }
}
