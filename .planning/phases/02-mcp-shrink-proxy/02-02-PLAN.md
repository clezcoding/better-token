---
phase: 02-mcp-shrink-proxy
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/shrink-mcp/src/config.ts
  - packages/shrink-mcp/src/index.ts
  - packages/shrink-mcp/tests/unit/config.test.ts
  - packages/shrink-mcp/tests/integration/proxy.test.ts
  - packages/core/src/cli.ts
autonomous: true
requirements:
  - MCP-04
user_setup: []

must_haves:
  truths:
    - "User can set `BETTER_TOKEN_SHRINK_FIELDS` to an allowlist of `tools.description`, `prompts.description`, and/or `resources.description` and only those list types have descriptions compressed (MCP-04, D-10, D-11)."
    - "When `BETTER_TOKEN_SHRINK_FIELDS` is unset, defaults equal D-09: all three field ids enabled (D-09, Claude discretion default string)."
    - "Invalid or unknown field tokens produce exactly one stderr warning and fall back to D-09 defaults; proxy still starts (D-12)."
    - "Mode is read from `BETTER_TOKEN_MODE` when CLI `--mode` omitted; when both set, CLI `--mode` wins (D-06, A3)."
    - "All proxy-related env vars use the `BETTER_TOKEN_*` prefix (D-11)."
    - statement: "Empty `BETTER_TOKEN_SHRINK_FIELDS` (whitespace-only or empty string) triggers D-12 warning + D-09 defaults."
      verification: backstop
  artifacts:
    - path: "packages/shrink-mcp/src/config.ts"
      provides: "parseShrinkFields + full parseProxyConfig env merge"
    - path: "packages/shrink-mcp/tests/unit/config.test.ts"
      provides: "MCP-04 + D-12 automated coverage"
  key_links:
    - from: "BETTER_TOKEN_SHRINK_FIELDS env"
      to: "shrinkListResponse fields Set"
      via: "parseProxyConfig → runProxy config.shrinkFields"
    - from: "CLI --mode"
      to: "config.mode"
      via: "overrides BETTER_TOKEN_MODE when present"
  prohibitions:
    - statement: "MUST NOT silently ignore invalid BETTER_TOKEN_SHRINK_FIELDS — always warn once on stderr then use defaults."
      flagged: true
    - statement: "MUST NOT accept field selectors outside tools.description / prompts.description / resources.description (no inputSchema or name shrinking)."
      flagged: true

assumptions:
  - id: A-edge-MCP-04-unclassified
    from: specless-probe
    text: "Empty or whitespace-only BETTER_TOKEN_SHRINK_FIELDS treated as invalid → D-12 defaults (unclassified probe resolved as planner assumption)."
  - id: A3-mode-precedence
    from: 02-RESEARCH.md / CONTEXT discretion
    text: "CLI --mode overrides BETTER_TOKEN_MODE when both set."
---

## Phase Goal

**As a** developer using an MCP-capable IDE, **I want to** route any MCP server through `better-token` and get smaller tool/prompt/resource descriptions, **so that** I save tokens without breaking tool calls.

<objective>
Make shrink field selection and compression mode fully configurable via `BETTER_TOKEN_*` environment variables (and CLI `--mode`), so users can disable compression per list type without code changes.

Purpose: Close MCP-04 and D-06/D-10/D-11/D-12 on top of the working proxy from 02-01.
Output: Complete `parseProxyConfig` / `parseShrinkFields`, unit + integration tests for field allowlist and invalid-config fallback.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mcp-shrink-proxy/02-CONTEXT.md
@.planning/phases/02-mcp-shrink-proxy/02-RESEARCH.md
@.planning/phases/02-mcp-shrink-proxy/02-PATTERNS.md
@.planning/phases/02-mcp-shrink-proxy/02-01-SUMMARY.md
@.planning/REQUIREMENTS.md

<interfaces>
From 02-01 (must already exist):
ProxyConfig, runProxy, shrinkListResponse, parseProxyConfig stub

DEFAULT_SHRINK_FIELDS string (exact):
tools.description,prompts.description,resources.description

Allowed field tokens only:
tools.description | prompts.description | resources.description

Env vars (D-11):
BETTER_TOKEN_SHRINK_FIELDS
BETTER_TOKEN_MODE
BETTER_TOKEN_DEBUG
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing config tests for MCP-04 field allowlist and D-12 invalid fallback</name>
  <files>
    packages/shrink-mcp/tests/unit/config.test.ts,
    packages/shrink-mcp/tests/integration/proxy.test.ts
  </files>
  <read_first>
    .planning/phases/02-mcp-shrink-proxy/02-CONTEXT.md (D-09..D-12, D-06, Claude discretion on CLI vs env),
    .planning/phases/02-mcp-shrink-proxy/02-RESEARCH.md (Config Parsing example, Assumptions A3),
    .planning/phases/02-mcp-shrink-proxy/02-PATTERNS.md (config.ts zod/env pattern),
    packages/shrink-mcp/src/config.ts (stub from 02-01)
  </read_first>
  <behavior>
    - MCP-04: parseProxyConfig with BETTER_TOKEN_SHRINK_FIELDS=`tools.description` yields shrinkFields Set size 1 containing only tools.description; prompts/resources not present.
    - MCP-04 integration: with env fields=`tools.description` only, proxy compresses tools/list descriptions but leaves prompts/list and resources/list descriptions byte-identical to mock originals.
    - D-12: unknown token `tools.name` or garbage string → stderr contains warning about invalid BETTER_TOKEN_SHRINK_FIELDS; returned fields equal full D-09 default set; no throw / no process.exit from parseShrinkFields.
    - D-06/A3: input `{ cliMode: "aggressive" }` with env BETTER_TOKEN_MODE=safe → config.mode === "aggressive".
    - D-06: cliMode omitted, env BETTER_TOKEN_MODE=safe → config.mode === "safe".
    - Default: no env, no cliMode → mode balanced + all three fields.
  </behavior>
  <action>
    Add packages/shrink-mcp/tests/unit/config.test.ts covering the behaviors above (capture stderr by stubbing process.stderr.write or spawning with env). Extend integration/proxy.test.ts with an MCP-04 case that sets BETTER_TOKEN_SHRINK_FIELDS=tools.description for the proxy child only. Tests should fail RED until Task 2 implements real parsing (if stub already returns all fields, assertions on single-field env MUST fail).
  </action>
  <verify>
    <automated>npm test --workspace=@better-token/shrink-mcp -- -t "MCP-04" ; expect non-zero exit (RED) if implementation incomplete</automated>
  </verify>
  <acceptance_criteria>
    - config.test.ts exists with cases tagged MCP-04 and D-12.
    - integration proxy.test.ts includes case tagged MCP-04.
    - Running MCP-04 tests fails RED before Task 2 lands (or documents already-green only if Task 2 completed in same commit — prefer separate RED commit).
  </acceptance_criteria>
  <done>
    RED tests specify MCP-04 allowlist, D-12 fallback, and CLI-over-env mode precedence.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement parseShrinkFields + full parseProxyConfig env/CLI merge (GREEN)</name>
  <files>
    packages/shrink-mcp/src/config.ts,
    packages/shrink-mcp/src/index.ts,
    packages/core/src/cli.ts,
    packages/shrink-mcp/tests/unit/config.test.ts,
    packages/shrink-mcp/tests/integration/proxy.test.ts
  </files>
  <read_first>
    .planning/phases/02-mcp-shrink-proxy/02-RESEARCH.md (Config Parsing section — DEFAULT_SHRINK_FIELDS, parseShrinkFields),
    .planning/phases/02-mcp-shrink-proxy/02-PATTERNS.md (config.ts Adapt section),
    packages/shrink-mcp/src/config.ts,
    packages/core/src/cli.ts (proxy subcommand from 02-01)
  </read_first>
  <behavior>
    - parseShrinkFields(raw): trim; split on commas; filter to allowed set; if selected.size===0 after filter → one stderr warning + return Set of all three defaults (D-12).
    - Unknown tokens discarded; if at least one valid token remains, use only valids (no warning required for mixed valid+invalid — discretion: warn if any unknown present OR only when empty selection; pick warn-on-any-unknown for transparency).
    - parseProxyConfig reads process.env.BETTER_TOKEN_SHRINK_FIELDS, BETTER_TOKEN_MODE, BETTER_TOKEN_DEBUG.
    - mode resolution: cliMode if provided and valid → else BETTER_TOKEN_MODE if valid → else balanced. Invalid mode string → stderr warning + balanced (align with D-12 spirit; do not crash).
    - debug: true if input.debug===true OR BETTER_TOKEN_DEBUG is "1" or "true" (case-insensitive) (D-16).
    - Export parseShrinkFields from index.ts for unit testing.
  </behavior>
  <action>
    Replace config stub with full implementation per D-09..D-12 and D-06/D-11/D-16. Keep ProxyConfig shape stable for runProxy. Ensure cli.ts proxy action still passes cliMode/debug/upstream from Commander; do not re-parse argv for env (config reads env itself). Prefer zod for mode enum validation consistent with Phase 1 cli.ts OptionsSchema. Make all MCP-04 and D-12 tests GREEN. Do not change shrink algorithm beyond using config.shrinkFields already wired in 02-01.
  </action>
  <verify>
    <automated>npm test --workspace=@better-token/shrink-mcp -- -t "MCP-04" &amp;&amp; npm test --workspace=@better-token/shrink-mcp -- -t "D-12"</automated>
  </verify>
  <acceptance_criteria>
    - npm test --workspace=@better-token/shrink-mcp -- -t "MCP-04" green.
    - npm test --workspace=@better-token/shrink-mcp -- -t "D-12" green — invalid fields warn once and default.
    - BETTER_TOKEN_SHRINK_FIELDS=tools.description leaves prompts/resources descriptions unchanged in integration test.
    - CLI --mode aggressive with BETTER_TOKEN_MODE=safe yields aggressive in unit test (A3).
    - No new external dependencies added to package.json.
  </acceptance_criteria>
  <done>
    Users configure compressible fields and mode via BETTER_TOKEN_* env (+ CLI --mode override); MCP-04 complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mcp.json / process env → parseProxyConfig | Operator-controlled local config; untrusted strings |
| parseProxyConfig → shrinkListResponse | Allowlist gate before any mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | BETTER_TOKEN_SHRINK_FIELDS | medium | mitigate | Allowlist-only tokens; unknown → warn + safe defaults (D-12); never interpret as code |
| T-02-07 | Elevation of Privilege | BETTER_TOKEN_MODE | low | mitigate | Enum zod/safeParse; invalid → balanced default |
| T-02-08 | Information Disclosure | stderr warnings | low | accept | Local stderr only; no secrets in field names |
| T-02-SC | Tampering | npm installs | low | accept | No new packages in this plan |

</threat_model>

## Artifacts this phase produces

### Config API (this plan)
- `DEFAULT_SHRINK_FIELDS` — `"tools.description,prompts.description,resources.description"`
- `parseShrinkFields(raw: string | undefined): Set<string>` — allowlist parser with D-12 fallback
- `parseProxyConfig` — full env + CLI merge (mode precedence: CLI &gt; env &gt; balanced)

### Env contract (user-facing)
- `BETTER_TOKEN_SHRINK_FIELDS` — comma-separated allowlist (MCP-04)
- `BETTER_TOKEN_MODE` — safe|balanced|aggressive
- `BETTER_TOKEN_DEBUG` — `1` / `true` enables debug

<verification>
MCP-04 and D-12 tests green; prior MCP-01/MCP-02 suite still green.
</verification>

<success_criteria>
1. Field allowlist env controls which list types shrink (MCP-04).
2. Invalid config warns once and uses D-09 defaults (D-12).
3. CLI --mode overrides BETTER_TOKEN_MODE (D-06 / A3).
</success_criteria>

<output>
Create `.planning/phases/02-mcp-shrink-proxy/02-02-SUMMARY.md` when done
</output>
