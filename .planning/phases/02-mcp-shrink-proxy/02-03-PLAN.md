---
phase: 02-mcp-shrink-proxy
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - packages/shrink-mcp/src/framing.ts
  - packages/shrink-mcp/src/proxy.ts
  - packages/shrink-mcp/src/shrink.ts
  - packages/shrink-mcp/tests/unit/framing.test.ts
  - packages/shrink-mcp/tests/integration/proxy.test.ts
  - packages/core/src/cli.ts
autonomous: false
requirements:
  - MCP-03
user_setup: []

must_haves:
  truths:
    - "When an upstream stdout line fails JSON.parse, the proxy writes the original line bytes to stdout (pass-through) and emits a single stderr diagnostic line — no data loss (MCP-03, D-13)."
    - "Parse-error stderr fires even when debug is off (D-13 always-on for pass-through notice); successful shrink token stats only when debug enabled (D-14, D-16)."
    - "When upstream exits with non-zero code, proxy process exits with the same code and a short stderr message (D-15)."
    - "JSON-RPC batch arrays (JSON.parse yields Array) pass through original line unchanged (research A2)."
    - "Partial trailing data without newline on upstream stdout close is flushed via pass-through (no silent drop)."
    - statement: "If interrupted mid-line (upstream close with buffered incomplete line), flush() emits remaining bytes as pass-through — concurrency/interrupt guarantee for MCP-02/MCP-03."
      verification: backstop
  artifacts:
    - path: "packages/shrink-mcp/src/proxy.ts"
      provides: "parse-error pass-through, exit-code propagation, debug shrink stats"
    - path: "packages/shrink-mcp/tests/integration/proxy.test.ts"
      provides: "MCP-03 + D-15 automated coverage"
  key_links:
    - from: "JSON.parse failure"
      to: "process.stdout original line"
      via: "writeNdjsonLine / raw write + stderr once"
    - from: "upstream exit code"
      to: "process.exit(code)"
      via: "D-15 handler"
  prohibitions:
    - statement: "MUST NOT drop or rewrite upstream bytes on parse failure — pass-through original line only."
      flagged: true
    - statement: "MUST NOT print shrink success diagnostics on stdout or when debug is disabled."
      flagged: true

assumptions:
  - id: A-edge-MCP-03-unclassified
    from: specless-probe
    text: "Unclassified MCP-03 edge resolved as: incomplete final buffer on upstream close → flush pass-through (no parse attempt required if no newline)."
  - id: A2-batch
    from: 02-RESEARCH.md
    text: "Batch JSON-RPC arrays pass through unchanged in v1."
---

## Phase Goal

**As a** developer using an MCP-capable IDE, **I want to** route any MCP server through `better-token` and get smaller tool/prompt/resource descriptions, **so that** I save tokens without breaking tool calls.

<objective>
Harden the proxy for real-world stream failures: parse-error pass-through (MCP-03), upstream exit-code propagation (D-15), debug-gated shrink diagnostics (D-14/D-16), batch/partial-line safety, then human-verify IDE transparency.

Purpose: Close success criterion 4 (parse fallback + env already in 02-02) and operational reliability without breaking the MCP stdout contract.
Output: Green MCP-03/D-15 tests, stderr-only diagnostics, optional human IDE check.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mcp-shrink-proxy/02-CONTEXT.md
@.planning/phases/02-mcp-shrink-proxy/02-RESEARCH.md
@.planning/phases/02-mcp-shrink-proxy/02-PATTERNS.md
@.planning/phases/02-mcp-shrink-proxy/02-01-SUMMARY.md
@.planning/phases/02-mcp-shrink-proxy/02-02-SUMMARY.md
@.planning/phases/02-mcp-shrink-proxy/02-VALIDATION.md

<interfaces>
From prior plans:
runProxy, NdjsonReadBuffer.flush, parseProxyConfig.debug, writeNdjsonLine

D-13 stderr example shape (wording discretionary):
better-token proxy: pass-through: parse error

D-15 stderr example shape:
better-token proxy: upstream exited with code &lt;N&gt;

D-14 debug shrink line (only when debug):
include estimated token before/after for that list response (bpe-lite via core or encode import); label estimated
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP-03 parse pass-through + D-15 exit propagation + framing edge tests (RED then GREEN)</name>
  <files>
    packages/shrink-mcp/src/framing.ts,
    packages/shrink-mcp/src/proxy.ts,
    packages/shrink-mcp/src/shrink.ts,
    packages/shrink-mcp/tests/unit/framing.test.ts,
    packages/shrink-mcp/tests/integration/proxy.test.ts
  </files>
  <read_first>
    .planning/phases/02-mcp-shrink-proxy/02-CONTEXT.md (D-13, D-14, D-15, D-16),
    .planning/phases/02-mcp-shrink-proxy/02-RESEARCH.md (Pitfalls 1–5, Pattern 3 handleUpstreamLine, Anti-Patterns batch),
    .planning/phases/02-mcp-shrink-proxy/02-VALIDATION.md (MCP-03, D-15 rows),
    packages/shrink-mcp/src/proxy.ts,
    packages/shrink-mcp/src/framing.ts
  </read_first>
  <behavior>
    - MCP-03: mock upstream writes a non-JSON line then a valid tools/list line; proxy stdout contains the non-JSON line unchanged AND a valid shrunk tools/list; stderr contains a pass-through parse notice (exactly one per bad line).
    - D-13: parse-error notice appears with debug=false (always-on for errors).
    - D-14: with debug=true and successful shrink, stderr contains estimated before/after token figures; with debug=false, no shrink-success stats lines.
    - D-15: mock upstream process.exit(7) after initialize → proxy exit code 7; stderr mentions upstream exit.
    - Batch: upstream line that is a JSON array of two response objects → proxy stdout line equals original bytes (no shrink attempt).
    - Framing: push partial chunks without newline keeps buffer; flush after end returns remainder; integration closes upstream mid-buffer and expects remainder pass-through.
    - nextCursor on paginated tools/list result preserved untouched while descriptions still shrink (open question recommendation).
  </behavior>
  <action>
    Extend framing unit tests first (RED if gaps). Implement/adjust proxy.ts: on catch JSON.parse → always stderr one pass-through line (D-13) + write original; on Array.isArray(msg) → pass-through original; on upstream close call reader.flush() and pass-through remainder if any; on upstream exit propagate code (D-15) with short stderr; when config.debug and shrink changed anything, emit estimated token stats to stderr only (D-14) using bpe-lite encode lengths. Keep single stdout consumer (Pitfall 1). Do not use MCP SDK. Ensure tools/call path still byte-identical. Make MCP-03 and D-15 tests GREEN along with framing edge tests.
  </action>
  <verify>
    <automated>npm test --workspace=@better-token/shrink-mcp -- -t "MCP-03" &amp;&amp; npm test --workspace=@better-token/shrink-mcp -- -t "D-15" &amp;&amp; npm test --workspace=@better-token/shrink-mcp</automated>
  </verify>
  <acceptance_criteria>
    - npm test --workspace=@better-token/shrink-mcp -- -t "MCP-03" green — invalid JSON line pass-through + stderr notice; subsequent valid list still shrinks.
    - npm test --workspace=@better-token/shrink-mcp -- -t "D-15" green — proxy exits with upstream non-zero code.
    - npm test --workspace=@better-token/shrink-mcp -- -t "D-14" green — debug stats only when debug enabled; stats on stderr not stdout.
    - Batch pass-through unit or integration assertion green.
    - Full workspace suite `npm test --workspace=@better-token/shrink-mcp` green.
    - Root `npm test` green (core + shrink-mcp).
    - grep for console.log in packages/shrink-mcp/src is empty (use stderr.write only).
  </acceptance_criteria>
  <done>
    MCP-03 parse fallback, D-13/D-14/D-15/D-16 diagnostics contract, batch/partial-line safety all green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify IDE connects through proxy transparently</name>
  <files>packages/core/src/cli.ts</files>
  <read_first>
    .planning/phases/02-mcp-shrink-proxy/02-VALIDATION.md (Manual-Only Verifications table),
    .planning/phases/02-mcp-shrink-proxy/02-CONTEXT.md (IDE mcp.json mental model)
  </read_first>
  <what-built>
    Automated suite covers mock-upstream stdio proxy for MCP-01..MCP-04. Live IDE connection (Claude Code / Cursor mcp.json) cannot be fully automated in CI — this checkpoint confirms transparent real-IDE use.
  </what-built>
  <how-to-verify>
    1. Build: `npm run build` at repo root (core + shrink-mcp).
    2. Pick any local MCP server you already use (or `npx @modelcontextprotocol/server-filesystem /tmp` if available).
    3. Point IDE mcp.json entry to: command = path to `better-token` (or `npx tsx packages/core/src/cli.ts`), args = `["proxy", "--", "&lt;upstream-cmd&gt;", "..."]`, optional env `BETTER_TOKEN_DEBUG=1`.
    4. Restart MCP / reload IDE; confirm tools list loads.
    5. Confirm a tool call still succeeds (round-trip works).
    6. Optional: with debug=1, check IDE MCP logs or terminal for stderr shrink estimated lines — never protocol errors from stdout pollution.
  </how-to-verify>
  <action>
    Pause for human verification per steps above. Do not change code unless user reports failures — if failures, fix in a follow-up commit before closing phase.
  </action>
  <verify>
    <human-check>User confirms IDE tools list loads through proxy and a tools/call succeeds</human-check>
  </verify>
  <acceptance_criteria>
    - User types `approved` after confirming tools list loads and at least one tool call works through `better-token proxy`.
    - Or user describes a concrete failure for the executor to fix before phase close.
  </acceptance_criteria>
  <done>
    Human confirmed transparent IDE proxy path (VALIDATION.md manual row) or filed a concrete defect.
  </done>
  <resume-signal>Type `approved` if IDE proxy works, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream stdout (possibly malformed) → proxy | Hostile/buggy upstream lines |
| Proxy stderr → operator terminal | Local only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | proxy stdout | high | mitigate | Parse errors and debug stats never written to stdout (D-13/D-14) |
| T-02-09 | Denial of Service | partial-line buffer | medium | mitigate | 4MB cap from 02-01; flush on close; no unbounded retry loops |
| T-02-10 | Repudiation | exit codes | low | mitigate | Propagate upstream exit code + stderr reason (D-15) |
| T-02-SC | Tampering | npm installs | low | accept | No new packages |

</threat_model>

## Artifacts this phase produces

### Resilience behaviors (this plan)
- Parse-error pass-through path in `runProxy` / `handleUpstreamLine` (MCP-03, D-13)
- `NdjsonReadBuffer.flush` used on upstream close
- Upstream exit-code propagation (D-15)
- Debug-gated estimated shrink stats on stderr (D-14, D-16)
- Batch array pass-through (A2)

### Full phase public surface (rollup)
- `runProxy`, `parseProxyConfig`, `parseShrinkFields`, `ProxyConfig`
- `NdjsonReadBuffer`, `writeNdjsonLine`
- `compressDescription`, `shrinkListResponse`, `MIN_DESCRIPTION_LENGTH`
- CLI: `better-token proxy -- &lt;upstream…&gt;` with `--mode`, `--debug`
- Env: `BETTER_TOKEN_SHRINK_FIELDS`, `BETTER_TOKEN_MODE`, `BETTER_TOKEN_DEBUG`

<verification>
Full shrink-mcp suite green; human IDE checkpoint approved or defect filed.
</verification>

<success_criteria>
1. Parse errors pass through with stderr notice (MCP-03, D-13).
2. Upstream non-zero exit propagates (D-15).
3. Debug stats only when enabled, stderr only (D-14, D-16).
4. Human confirms IDE transparent connect (success criterion 1).
</success_criteria>

<output>
Create `.planning/phases/02-mcp-shrink-proxy/02-03-SUMMARY.md` when done
</output>
