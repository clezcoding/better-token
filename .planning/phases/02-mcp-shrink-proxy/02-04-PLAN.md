---
phase: 02-mcp-shrink-proxy
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
gap_closure: true
gap_ids:
  - G-02-2
files_modified:
  - packages/core/src/compressor.ts
  - packages/core/tests/unit/compressor.test.ts
  - packages/core/tests/unit/mcp-descriptions.test.ts
  - packages/core/tests/fixtures/filesystem-tools-descriptions.json
  - packages/shrink-mcp/tests/fixtures/mock-upstream.ts
  - packages/shrink-mcp/tests/fixtures/mock-upstream-filesystem.ts
  - packages/shrink-mcp/tests/unit/shrink.test.ts
  - packages/shrink-mcp/tests/integration/proxy.test.ts
  - .cursor/mcp.json
autonomous: true
requirements:
  - MCP-01

must_haves:
  truths:
    - "Balanced L1 compresses real @modelcontextprotocol/server-filesystem tool descriptions with measurable char savings (not 4108→4108 no-op)."
    - "Every compressed filesystem description passes compressMarkdownWithValidation with validation.ok=true (D-07)."
    - "Proxy tools/list through shrink path returns smaller descriptions for technical MCP prose, not only filler mock text."
    - "Mock fixtures include real-style technical descriptions so tests cannot pass on filler alone."
    - "Demo mcp.json entry makes Token sparen visible without breaking tools/call."
  artifacts:
    - path: packages/core/tests/fixtures/filesystem-tools-descriptions.json
      provides: "Frozen corpus of 14 live filesystem MCP tool descriptions"
    - path: packages/core/src/compressor.ts
      provides: "Balanced-mode MCP technical prose patterns (D-05)"
    - path: packages/shrink-mcp/tests/fixtures/mock-upstream-filesystem.ts
      provides: "Stdio mock upstream serving filesystem corpus for integration tests"
    - path: .cursor/mcp.json
      provides: "Dual demo entries — live filesystem + mock with visible shrink"
  key_links:
    - from: compressBalanced in compressor.ts
      to: compressDescription in shrink.ts
      via: compressMarkdownWithValidation unchanged import path (D-05)
    - from: filesystem-tools-descriptions.json
      to: mcp-descriptions.test.ts
      via: corpus-level before/after char count gate
    - from: mock-upstream-filesystem.ts
      to: proxy.test.ts
      via: spawn proxy -- mock upstream; assert shrunk tools/list
---

## Phase Goal

**As a** developer using an MCP-capable IDE, **I want to** route any MCP server through `better-token` and get smaller tool/prompt/resource descriptions, **so that** I save tokens without breaking tool calls.

<objective>
Close G-02-2: extend balanced L1 heuristics so dense technical MCP tool descriptions (not just English filler) shrink measurably, backed by a real filesystem corpus fixture and updated demo config.

Purpose: UAT Test 2 failed because BALANCED_FILLERS never matched filesystem MCP prose; mock fixtures gave false confidence. Fix L1 coverage, not proxy wiring.
Output: Passing corpus tests, updated mocks, dual demo mcp.json entries with visible savings.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mcp-shrink-proxy/02-UAT.md
@.planning/debug/mcp-live-zero-token-savings.md
@.planning/phases/02-mcp-shrink-proxy/02-CONTEXT.md
@.planning/phases/02-mcp-shrink-proxy/02-PATTERNS.md
@packages/core/src/compressor.ts
@packages/shrink-mcp/src/shrink.ts
@packages/shrink-mcp/tests/fixtures/mock-upstream.ts
@.cursor/mcp.json

<interfaces>
From packages/core/src/compressor.ts (extend, do not replace):
compressProse(prose: string, mode: CompressionMode): string
compressMarkdownWithValidation(content: string, mode: CompressionMode): { content: string; validation: ValidationResult }

From packages/shrink-mcp/src/shrink.ts (unchanged API):
compressDescription(text: string, mode: CompressionMode): { text: string; changed: boolean }
MIN_DESCRIPTION_LENGTH = 48

Gap root cause (G-02-2): live filesystem corpus 4108 chars, balanced/aggressive unchanged, validation.ok=true — L1 coverage gap only.
Recurring patterns in live corpus (capture exact strings into fixture, use as regex targets):
- Trailing scope suffix on most tools: "Only works within allowed directories."
- Preamble: "Use this tool when you need to "
- Qualifier prefixes: "This tool is essential for ", "This tool is useful for ", "This tool is perfect for "
- Verbosity: "Read the complete contents of ", " from the file system", "Get a detailed listing of "
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze filesystem MCP corpus + failing G-02-2 corpus test</name>
  <files>
    packages/core/tests/fixtures/filesystem-tools-descriptions.json,
    packages/core/tests/unit/mcp-descriptions.test.ts
  </files>
  <behavior>
    - Fixture contains exactly 14 tool records from live @modelcontextprotocol/server-filesystem tools/list (name + description strings only).
    - G-02-2 corpus test: sum description char lengths before balanced compress ≥ 4000; after compress &lt; before; savings ≥ 8%; at least 8 of 14 tools have changed descriptions.
    - Per-tool: compressMarkdownWithValidation returns validation.ok=true for every description.
    - Test MUST fail on current compressor (4108→4108) before Task 2.
  </behavior>
  <action>
    Capture live corpus: spawn `npx -y @modelcontextprotocol/server-filesystem /tmp`, send initialize + notifications/initialized + tools/list NDJSON lines, extract result.tools[].name and result.tools[].description into packages/core/tests/fixtures/filesystem-tools-descriptions.json (array of {name, description} objects). Commit frozen snapshot — do not fetch at test runtime.

    Create packages/core/tests/unit/mcp-descriptions.test.ts with describe "G-02-2: filesystem MCP description corpus". Import fixture, loop descriptions through compressMarkdownWithValidation in balanced mode. Assert aggregate char savings ≥ 8%, toolsChanged ≥ 8, every validation.ok true. Export helper sumDescriptionChars(descriptions) for reuse in shrink-mcp tests.

    Do NOT add new npm packages. No proxy changes in this task.
  </action>
  <verify>
    <automated>npm test --workspace=@better-token/core -- tests/unit/mcp-descriptions.test.ts</automated>
  </verify>
  <done>Failing RED test exists proving current balanced mode saves 0% on filesystem corpus; fixture committed with 14 real descriptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend balanced L1 with MCP technical prose patterns (D-05, D-07)</name>
  <files>
    packages/core/src/compressor.ts,
    packages/core/tests/unit/compressor.test.ts
  </files>
  <behavior>
    - G-02-2 corpus test turns GREEN: ≥8% char savings, ≥8 tools changed, all validation.ok.
    - Existing COMP-02 filler tests still pass (BALANCED_FILLERS unchanged behavior).
    - SAFE-01: validator failure still returns original bytes.
    - Deterministic: two compressProse calls on same input produce identical output.
  </behavior>
  <action>
    In packages/core/src/compressor.ts, add BALANCED_MCP_PATTERNS: RegExp[] applied inside compressBalanced AFTER existing BALANCED_FILLERS + normalizeWhitespace (per D-05 — still balanced mode, not a new mode). Use processLinesPreservingPlaceholders wrapper like fillers.

    Minimum pattern set (tune against fixture — add more only if needed to hit 8% gate without validator failures):
    1. /\s*Only works within allowed directories\.?/gi — remove repeated scope suffix
    2. /Use this tool when you need to /gi — remove preamble
    3. /This tool is (essential|useful|perfect) for /gi — remove qualifier prefix
    4. /Read the complete contents of /gi → Read 
    5. / from the file system/gi — remove redundant phrase
    6. /Get a detailed listing of /gi → List 

    After each pattern batch, run normalizeWhitespace once. Do NOT strip inline code, URLs, paths, or headings — descriptions are plain prose so tokenizeMarkdown is identity; validator still gates output.

  Add compressor.test.ts cases: "G-02-2: balanced shortens filesystem scope suffix", "G-02-2: balanced shortens Use this tool preamble" with inline strings from fixture. Re-run mcp-descriptions.test.ts until GREEN.

  Do NOT weaken validator. Do NOT add LLM rewrite. Aggressive mode inherits via compressBalanced call chain — no separate aggressive patterns required unless corpus gate still fails (then extend BALANCED_MCP_PATTERNS only).
  </action>
  <verify>
    <automated>npm test --workspace=@better-token/core -- tests/unit/mcp-descriptions.test.ts tests/unit/compressor.test.ts</automated>
  </verify>
  <done>Balanced L1 shrinks filesystem corpus ≥8% with validator pass on all 14 tools; existing Phase 1 compressor tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Realistic mocks, proxy integration gate, dual demo mcp.json</name>
  <files>
    packages/shrink-mcp/tests/fixtures/mock-upstream.ts,
    packages/shrink-mcp/tests/fixtures/mock-upstream-filesystem.ts,
    packages/shrink-mcp/tests/unit/shrink.test.ts,
    packages/shrink-mcp/tests/integration/proxy.test.ts,
    .cursor/mcp.json
  </files>
  <action>
    Update mock-upstream.ts: keep echo tool with existing LONG_DESCRIPTION (filler regression). Add second tool read_text_file whose description is copied verbatim from filesystem-tools-descriptions.json entry — proves shrink works on technical prose through proxy path, not filler-only.

    Create mock-upstream-filesystem.ts: stdio readline mock serving full tools/list from filesystem-tools-descriptions.json (all 14 tools with minimal inputSchema stubs). tools/call returns echo of request line (same pattern as mock-upstream.ts).

    shrink.test.ts: add test "G-02-2: compresses technical filesystem-style description" using read_text_file description from fixture; expect changed=true and shorter length.

    proxy.test.ts: add integration test "G-02-2: proxy shrinks filesystem corpus mock upstream" — spawn `npx tsx packages/core/src/cli.ts proxy -- npx tsx packages/shrink-mcp/tests/fixtures/mock-upstream-filesystem.ts`, send tools/list, parse response, assert sum(description lengths) &lt; fixture baseline sum by ≥8%. MCP-02 unchanged — existing byte-identical tools/call test must still pass.

    Update .cursor/mcp.json with dual entries (valid JSON, no comments):
    - better-token-proxy: keep filesystem upstream (now shows savings post-fix); keep BETTER_TOKEN_DEBUG=1
    - better-token-proxy-demo: upstream `npx tsx packages/shrink-mcp/tests/fixtures/mock-upstream-filesystem.ts` with same env — fast local demo of Token sparen without npx cold-start on filesystem package

    Do NOT remove better-token-proxy filesystem entry. Do NOT mutate tools/call payloads.
  </action>
  <verify>
    <automated>npm test --workspace=@better-token/shrink-mcp -- -t "G-02-2"</automated>
  </verify>
  <done>Proxy integration proves shrink on filesystem-style corpus; mocks mix filler + technical prose; mcp.json has filesystem + demo entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frozen fixture descriptions → L1 compressor | Untrusted-length prose input; regex transforms must not corrupt protected regions |
| Proxy stdout → IDE | MCP JSON-RPC stream must stay valid after description mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering | BALANCED_MCP_PATTERNS | medium | mitigate | compressMarkdownWithValidation + per-field rollback (D-07); corpus test asserts validation.ok on all 14 |
| T-02-12 | Information Disclosure | mcp.json demo paths | low | accept | Demo uses repo-local mock fixture; filesystem entry unchanged |
| T-02-SC | Tampering | npm installs | low | accept | No new packages; filesystem capture uses existing npx one-shot |

</threat_model>

<verification>
npm test --workspace=@better-token/core && npm test --workspace=@better-token/shrink-mcp
Re-run UAT Test 2 mental check: A/B tools/list filesystem via proxy shows char reduction ≥8% (not 4108→4108).
</verification>

<success_criteria>
- G-02-2 closed: measurable description shrink on real filesystem MCP corpus in balanced mode
- MCP-01 satisfied for technical prose, not only filler mocks
- All 8 judgment prohibitions from UAT Test 3 remain unaffected (no stdout pollution, no tools/call mutation, validator gate intact)
</success_criteria>

<output>
Create .planning/phases/02-mcp-shrink-proxy/02-04-SUMMARY.md when done
</output>

<!-- Multi-Source Coverage Audit
SOURCE  | ID      | Feature/Requirement                              | Plan  | Status  | Notes
GOAL    | —       | Token sparen on list descriptions              | 02-04 | COVERED | Corpus + proxy gates
REQ     | MCP-01  | Compressed list descriptions                   | 02-04 | COVERED | Technical prose path
GAP     | G-02-2  | Extend L1 for dense MCP patterns               | 02-04 | COVERED | Task 2, D-05
GAP     | G-02-2  | Fixture from real filesystem descriptions      | 02-04 | COVERED | Task 1
GAP     | G-02-2  | Dual demo upstream for visible savings         | 02-04 | COVERED | Task 3 mcp.json
CONTEXT | D-05    | Reuse L1 balanced mode                         | 02-04 | COVERED | Task 2
CONTEXT | D-07    | Validator gate per description                 | 02-04 | COVERED | Task 1+2 asserts
RESEARCH| —       | compressMarkdownWithValidation for descriptions| 02-04 | COVERED | Unchanged wiring
-->
