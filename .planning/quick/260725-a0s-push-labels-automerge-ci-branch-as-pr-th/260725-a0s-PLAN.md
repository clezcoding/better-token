---
phase: quick-260725-a0s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/dependabot.yml
  - .github/labels.yml
  - .github/workflows/automerge-label.yml
  - .github/workflows/label-sync.yml
  - .github/workflows/mega-linter.yml
  - .kodiak.toml
  - packages/core/package.json
  - packages/core/vitest.config.ts
  - package-lock.json
  - .planning/quick/260725-a0s-push-labels-automerge-ci-branch-as-pr-th/260725-a0s-SUMMARY.md
autonomous: true
requirements:
  - QUICK-260725-a0s
user_setup: []
must_haves:
  truths:
    - "Branch contains cherry-picked labels/automerge/CI tooling from 70d7ced and MegaLinter PR-diff mode from cd77d6e"
    - "packages/core uses vitest 4.1.10 (latest stable); lockfile resolved; no vulnerable nested vite 5.x or esbuild 0.21.x"
    - "npm test and npm run build pass on the branch"
    - "SUMMARY documents disposition of Dependabot alerts #1–#6 and PRs #11–#13; notes zero open classic Issues"
    - "Branch is ready to push; no gh pr create performed by executor"
  artifacts:
    - path: .github/workflows/automerge-label.yml
      provides: "Automerge label on Dependabot/ImgBot PRs"
    - path: packages/core/package.json
      provides: "vitest ^4.1.10 (or 4.1.10) in core devDependencies"
    - path: package-lock.json
      provides: "Resolved vite ≥6.4.3 and no nested vulnerable esbuild under vite"
    - path: .planning/quick/260725-a0s-push-labels-automerge-ci-branch-as-pr-th/260725-a0s-SUMMARY.md
      provides: "Alert/PR disposition table + verification evidence"
  key_links:
    - from: packages/core/package.json vitest
      to: package-lock.json vite/esbuild tree
      via: "vitest 4 pulls vite ≥6.4.3 which clears nested esbuild alert"
    - from: .github/dependabot.yml labels
      to: .kodiak.toml automerge_dependencies
      via: "dependencies+automerge labels + major/minor/patch automerge"
---

<objective>
Prepare branch `gsd/quick-260725-a0s-push-labels-automerge-ci-branch-as-pr-th` (from origin/main) with cherry-picked labels/automerge/CI tooling plus a full Dependabot security remediation (vitest 4.1.10), verified by tests/build, documented in SUMMARY — ready for orchestrator to push and open the PR.

Purpose: Land 9tg tooling cleanly on main lineage and clear all open Dependabot alerts/PRs by version bump (not dismissal).
Output: Tooling + lockfile fix committed on branch; SUMMARY with every alert/PR disposition; no `gh pr create`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260725-a0s-push-labels-automerge-ci-branch-as-pr-th/260725-a0s-CONTEXT.md
@.planning/PROJECT.md

Branch (already checked out from origin/main): `gsd/quick-260725-a0s-push-labels-automerge-ci-branch-as-pr-th`
Source commits (from `gsd/quick-260725-9tg-github-labels-automerge-fix-security-dep`):
- `70d7ced` — labels, label-sync issues:write, Dependabot labels, Kodiak major automerge, automerge-label workflow
- `cd77d6e` — MegaLinter VALIDATE_ALL_CODEBASE PR-diff vs full-on-main

Current vulnerable tree on main lineage: vitest@2.1.9 → vite@5.4.21 → esbuild@0.21.5
Canonical: GHSA-5xrq-8626-4rwp (vitest); GHSA-v6wh-96g9-6wx3 / GHSA-fx2h-pf6j-xcff / GHSA-4w7w-66w2-5vf9 (vite); GHSA-67mh-4wv8-2f99 (esbuild)
Vitest migration: https://vitest.dev/guide/migration.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick labels/automerge/CI tooling onto main lineage</name>
  <files>.github/dependabot.yml,.github/labels.yml,.github/workflows/automerge-label.yml,.github/workflows/label-sync.yml,.github/workflows/mega-linter.yml,.kodiak.toml</files>
  <action>
Per CONTEXT PR strategy: stay on `gsd/quick-260725-a0s-push-labels-automerge-ci-branch-as-pr-th` (based on origin/main). Do not merge the 9tg branch (it forked from phase-02).

Cherry-pick in order:
1. `70d7ced` — labels expand (help wanted, invalid, question, priority:*), Dependabot `labels: [dependencies, automerge]` on npm + gha, label-sync `issues: write`, Kodiak `versions = ["major", "minor", "patch"]`, new `.github/workflows/automerge-label.yml`
2. `cd77d6e` — MegaLinter `VALIDATE_ALL_CODEBASE` becomes event-conditional (false on pull_request, true otherwise)

If a cherry-pick conflicts, resolve to keep main-lineage history and the intent of those two commits only — do not pull phase-02 package tree. After both apply cleanly, commit if cherry-pick did not auto-commit, using the original commit messages (or equivalent feat messages referencing quick-260725-a0s / 9tg source SHAs).

Do not open a GitHub PR. Do not push unless needed for later orchestrator step — prefer local commits only.
  </action>
  <verify>
    <automated>test -f .github/workflows/automerge-label.yml &amp;&amp; grep -q 'major' .kodiak.toml &amp;&amp; grep -q 'automerge' .github/dependabot.yml &amp;&amp; grep -q "github.event_name == 'pull_request'" .github/workflows/mega-linter.yml &amp;&amp; grep -q 'help wanted' .github/labels.yml &amp;&amp; grep -q 'issues: write' .github/workflows/label-sync.yml</automated>
  </verify>
  <done>Both cherry-picks applied; automerge-label workflow present; Dependabot/Kodiak/MegaLinter/labels match 9tg tooling intent on main lineage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bump vitest to 4.1.10 and clear Dependabot security tree</name>
  <files>packages/core/package.json,packages/core/vitest.config.ts,package-lock.json</files>
  <behavior>
    - After install: `packages/core` vitest resolves to 4.1.10 (range ^4.1.10 acceptable)
    - Lockfile vite under vitest is ≥6.4.3 (not 5.4.21)
    - No nested esbuild 0.21.x under vite (alert #1 path cleared)
    - `npm test` and `npm run build` exit 0
  </behavior>
  <action>
Per CONTEXT Security/Dependabot table: remediate alerts #6/#3 (vitest), #5/#4/#2 (vite transitive), #1 (esbuild nested) by bumping to latest stable — not by dismissing alerts.

In repo root, install into the core workspace only (main has no shrink-mcp): prefer `npm install vitest@4.1.10 -w @better-token/core` so package.json and package-lock.json update together. Ensure `packages/core/package.json` lists vitest at ^4.1.10 (or exact 4.1.10). Do not bump unrelated deps except what npm resolves as required peers of vitest 4.

Verify lockfile after install:
- `npm ls vitest vite esbuild -w @better-token/core` (or equivalent) shows vitest 4.1.10, vite ≥6.4.3 under vitest, and no vite→esbuild@0.21.x path
- Direct `esbuild` in core may already be ^0.28.1; ensure lockfile matches declared range (refresh if invalid)

Run `npm test` and `npm run build`. If vitest 4 breaks tests or config, fix minimally in `packages/core/vitest.config.ts` and/or test files (API/config only — see vitest migration guide). Do not leave vulnerable versions to make CI green.

Do not dismiss Dependabot alerts. Do not close Dependabot PRs #11/#12/#13 yet (orchestrator closes after new PR exists). Do not run `gh pr create`.
  </action>
  <verify>
    <automated>npm ls vitest -w @better-token/core --depth=0 | grep -q 'vitest@4.1.10' &amp;&amp; npm ls vite -w @better-token/core --all | grep -E 'vite@[6-9]\.|vite@[1-9][0-9]\.' | grep -v 'vite@5\.' | head -1 | grep -q vite &amp;&amp; ! npm ls esbuild -w @better-token/core --all | grep -E 'esbuild@0\.21\.' &amp;&amp; npm test &amp;&amp; npm run build</automated>
  </verify>
  <done>vitest 4.1.10 installed with refreshed lockfile; vite≥6.4.3; no vulnerable nested esbuild 0.21.x; test and build pass; alerts remediable by merge (not dismissed).</done>
</task>

<task type="auto">
  <name>Task 3: Write SUMMARY with alert/PR dispositions; leave branch push-ready</name>
  <files>.planning/quick/260725-a0s-push-labels-automerge-ci-branch-as-pr-th/260725-a0s-SUMMARY.md</files>
  <action>
Write SUMMARY documenting:

1. Cherry-pick SHAs applied (`70d7ced`, `cd77d6e`) and files touched for tooling.
2. Table for every Dependabot alert disposition (must cover all):
   - #6, #3 vitest → bumped to 4.1.10
   - #5, #4, #2 vite → cleared via vitest→vite ≥6.4.3 (record resolved vite version from lockfile)
   - #1 esbuild → cleared when nested 0.21.x path gone (record resolved esbuild versions)
3. Table for Dependabot PRs:
   - #11 (vite+vitest multi) — supersede after this PR lands
   - #12 (vitest → 3.2.6) — supersede (insufficient vs latest)
   - #13 (vitest → 4.1.10 core-only, CI failed / incomplete lock) — supersede (this branch completes lock + verifies)
4. Classic Issues: none open (document explicitly). Code scanning / secret scanning: none open.
5. Verification commands + results (`npm test`, `npm run build`, `npm ls` evidence).
6. Explicit note: executor did not dismiss alerts; did not `gh pr create`; branch ready for orchestrator push + PR. After PR opens, close #11/#12/#13 with comment linking the new PR (orchestrator/follow-up — note as post-PR step).

Commit tooling + dependency + SUMMARY changes on the branch with a clear message (feat/fix for a0s). Ensure working tree clean for those paths. Do not push; do not `gh pr create`.
  </action>
  <verify>
    <automated>test -f .planning/quick/260725-a0s-push-labels-automerge-ci-branch-as-pr-th/260725-a0s-SUMMARY.md &amp;&amp; grep -E '#(1|2|3|4|5|6)' .planning/quick/260725-a0s-push-labels-automerge-ci-branch-as-pr-th/260725-a0s-SUMMARY.md | head -1 &amp;&amp; grep -E '#(11|12|13)' .planning/quick/260725-a0s-push-labels-automerge-ci-branch-as-pr-th/260725-a0s-SUMMARY.md | head -1 &amp;&amp; grep -qi 'classic' .planning/quick/260725-a0s-push-labels-automerge-ci-branch-as-pr-th/260725-a0s-SUMMARY.md &amp;&amp; git rev-parse --abbrev-ref HEAD | grep -q 'gsd/quick-260725-a0s' &amp;&amp; ! git log -1 --format=%s | grep -qi 'gh pr create'</automated>
  </verify>
  <done>SUMMARY lists alerts #1–#6 and PRs #11–#13 with dispositions; notes zero classic Issues; branch clean and ready for orchestrator push/PR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → repo metadata | Workflows with `pull-requests: write` / `issues: write` act on labels using GITHUB_TOKEN |
| npm registry → lockfile | Dependency resolution for vitest/vite/esbuild must land patched versions |
| Dependabot bots → automerge path | Labels + Kodiak major automerge widen auto-merge surface for bot PRs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-a0s-01 | Tampering | vitest/vite/esbuild dependency tree | critical | mitigate | Bump vitest to 4.1.10; verify lockfile vite≥6.4.3 and no esbuild 0.21.x; tests/build must pass |
| T-a0s-02 | Elevation | .github/workflows/automerge-label.yml | medium | mitigate | Restrict label job to Dependabot/ImgBot actors only; use GITHUB_TOKEN with least privilege (contents:read, pull-requests:write) |
| T-a0s-03 | Tampering | Kodiak major automerge | medium | accept | Solo repo + required status checks on main; majors still need green CI before merge |
| T-a0s-04 | Information Disclosure | Dependabot alert handling | low | mitigate | Never dismiss alerts; remediate by version bump so GitHub auto-closes on merge |
| T-a0s-SC | Tampering | npm install vitest@4.1.10 | high | mitigate | Bump existing known package only (vitest on npm); verify version 4.1.10 in lockfile after install |
</threat_model>

<verification>
- Cherry-pick artifacts present and MegaLinter conditional VALIDATE_ALL_CODEBASE set
- vitest 4.1.10 + vite≥6.4.3 + no nested esbuild 0.21.x
- npm test && npm run build pass
- SUMMARY covers alerts #1–#6, PRs #11–#13, zero classic Issues
- No gh pr create by executor
</verification>

<success_criteria>
- Tooling from 70d7ced + cd77d6e on main-based branch
- All listed Dependabot alerts remediable via lockfile (not dismissed)
- Tests and build green on vitest 4.1.10
- SUMMARY complete; branch push-ready for orchestrator PR
</success_criteria>

<source_audit>
SOURCE    | ID              | Feature/Requirement                                      | Plan | Status  | Notes
--------- | --------------- | -------------------------------------------------------- | ---- | ------- | -----
GOAL      | —               | PR-ready branch: 9tg tooling + full Dependabot remediation | 01 | COVERED |
REQ       | QUICK-260725-a0s| Push labels-automerge-CI as PR; fix all security alerts   | 01 | COVERED |
CONTEXT   | PR strategy     | One PR from a0s branch; cherry-pick + security + docs    | 01 | COVERED | Task 1–3
CONTEXT   | Alerts #1–#6    | Bump vitest 4.1.10; verify vite/esbuild; no dismissals   | 01 | COVERED | Task 2
CONTEXT   | Dep PRs #11–#13 | Supersede after fix; document dispositions               | 01 | COVERED | Task 3
CONTEXT   | Classic Issues  | None open — document in SUMMARY                          | 01 | COVERED | Task 3
CONTEXT   | No gh pr create | Executor prepares branch only                            | 01 | COVERED | Tasks 1–3
</source_audit>

<output>
Create `.planning/quick/260725-a0s-push-labels-automerge-ci-branch-as-pr-th/260725-a0s-SUMMARY.md` when done.
Do not run `gh pr create` (orchestrator opens PR after execute).
</output>
